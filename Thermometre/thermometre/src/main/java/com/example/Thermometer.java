package com.example;

public class Thermometer
{
    public int Generate(int[] array) {
        if (array.length == 0 || array.length > 10000) {
            return 0;
        }

        return 1;
    }
}
